#slide-menu.sidenav.mobile-nav
  .menu
    ul.collection
      each item, index in menu.crypto
        li.collection-item(style="animation-duration: "+(Number(index + 1)) * 0.25 + 's')
          a.sidenav-close.waves-effect.menu-list(href='#' + item.name) #{$t.crypto_landing['header_'+item.name]}
      li.collection-item(style="animation-duration: "+menu.crypto.length * 0.25 + 's')
          a.waves-effect.menu-list(href=link.crypto.contact) #{$t.crypto_landing.header_contact}
    hr.divider-sidebar
    ul.collection
      each item, index in ['login', 'register']
        li.collection-item(style="animation-duration: "+menu.crypto.length * 0.25 + 's')
          a.sidenav-close.waves-effect.menu-list(href=item +".html") #{$t.crypto_landing['header_'+item]}